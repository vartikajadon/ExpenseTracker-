const axios = require('axios'); async function test(){ try { const aiPrompt = 'Return JSON'; const base64Image = Buffer.from('fake image').toString('base64'); const res = await axios.post('https://api.groq.com/openai/v1/chat/completions', { model: 'llama-3.2-90b-vision-preview', messages: [{ role: 'user', content: [{ type: 'text', text: aiPrompt }, { type: 'image_url', image_url: { url: 'data:image/png;base64,' + base64Image } }] }], temperature: 0.1 }, { headers: { 'Authorization': 'Bearer ' + process.env.GROQ_API_KEY, 'Content-Type': 'application/json' } }); console.log(res.data); } catch(err){ console.log(err.response ? err.response.data : err.message); } } test();  
